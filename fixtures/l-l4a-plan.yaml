endpoint: /qual-coder.planning
contains:
  - "GREETING MODE"
  - E2E-L4A-TOOLS
reply:
  toolCalls:
    - name: submit_plan
      args:
        task: "E2E-L4A task"
        steps:
          - { title: "inspect the tools", expected: "tool list captured" }
