endpoint: /qual-coder.planning
contains:
  - "GREETING MODE"
  - E2E-L4B-RETIRE
reply:
  toolCalls:
    - name: submit_plan
      args:
        task: "E2E-L4B task"
        steps:
          - { title: "the last and only step", expected: "plan retires" }
