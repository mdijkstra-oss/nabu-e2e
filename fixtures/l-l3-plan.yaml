endpoint: /qual-coder.planning
contains:
  - "GREETING MODE"
  - E2E-L3-PLANTOOLS
reply:
  toolCalls:
    - name: submit_plan
      args:
        task: "E2E-L3 single step task"
        steps:
          - { title: "the only step", expected: "it happens" }
