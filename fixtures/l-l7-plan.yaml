endpoint: /qual-coder.planning
contains:
  - "GREETING MODE"
  - E2E-L7-GUARD
reply:
  toolCalls:
    - name: submit_plan
      args:
        task: "E2E-L7 task"
        steps:
          - { title: "single step", expected: "completed once" }
